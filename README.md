# KDG Hytale Portal (Hytale News Tracker — Styled v6)

**Версия:** 1.3.2-KDG

Это десктопное приложение показывает новости и видео по Hytale и оформлено под стилистику https://hytale.com с фирменным логотипом, линией, кнопкой очистки кэша и ссылкой на KDG (https://bio.link/kdg_info).

## Особенности

- Новостной и видеосписок с миниатюрами, кнопками просмотра и статус-баром
- Кнопка "Новости из Telegram" для быстрого доступа к Telegram-каналу Автор:Джет Hytale
- Кэширование статей и переводов в `news_cache_v3.json`
- Поддержка прокрутки каналов и статей колесиком (колесо работает в любых областях окна статьи) и автоматическое открытие видео в браузере.
- Футер с ссылкой «Created by KDG» и авторским брендом.

## Требования

```bash
pip install -r requirements.txt
```

- Python 3.8 или выше (поддерживаются Windows, macOS, Linux)
- requests
- feedparser
- beautifulsoup4
- pillow
 - pywebview
 - pygame (for embedded playback inside the app)
 - mutagen (used by the embedded player to determine track duration)
 - pygame (for embedded playback inside the app)
 - mutagen (used by the embedded player to determine track duration)

`tkinter` уже встроен в Python.

## Установка и запуск

1. Клонируйте или скопируйте каталог проекта (должны быть `main.py`, `logo.png`, `requirements.txt`).
2. Создайте виртуальное окружение (`python -m venv .venv` и активируйте `\.venv\Scripts\activate` или `source .venv/bin/activate`).
3. Установите зависимости `pip install -r requirements.txt`.
4. Запустите `python main.py`.

После этого приложение откроет главное окно, загрузит новости и видео и позволит взаимодействовать с контентом.

## История версий

### Версия 1.3.2 (Текущая)
- Улучшена загрузка изображений с сайта Hytale
- Добавлена обработка ошибок при загрузке изображений
- Улучшено отображение миниатюр YouTube
- Исправлены проблемы с кэшированием изображений
- Обновлены HTTP-заголовки для предотвращения ошибки 403

### Версия 1.3.1
- Добавлена кнопка "Новости из Telegram" с иконкой для быстрого доступа к каналу (новости взяты из Телеграмма: Джет Hytale)
- Обновлен интерфейс кнопок управления

### Изменения в версии 1.3.0:
- Добавлен встроенный музыкальный плеер с поддержкой плейлистов
- Добавлены кнопки управления воспроизведением
- Реализовано кэширование избранных треков

### Изменения в версии 1.2.2:
- Улучшено управление музыкой: кнопка "Музыка: Вкл/Выкл" теперь корректно включает и выключает воспроизведение
- Улучшен интерфейс: кнопки управления музыкой выровнены по центру под кнопкой очистки кэша
- Добавлена музыкальная нота 🎵 к кнопке открытия плеера
- Исправлены ошибки в работе аудиоплеера

### Изменения в версии 1.2.1:

Приложение включает в себя полноценный музыкальный плеер со следующими возможностями:

- Воспроизведение музыки из папки `Music/` при запуске
- Поддерживаемые форматы: MP3, OGG, WAV, FLAC, AAC, M4A
- Начальная громкость 21% по умолчанию
- Управление воспроизведением с главного экрана:
  - Кнопка включения/выключения звука
  - Быстрый доступ к плееру
- Полноценное окно плеера с:
  - Управлением воспроизведением (вперед/назад, пауза/воспроизведение)
  - Ползунком перемотки трека
  - Плавной регулировкой громкости с отображением процентов
  - Список воспроизведения и избранные треки
  - Таймер с отображением текущего времени и длительности трека
- Автоматическое сохранение избранных треков в `music_favorites.json`
- Запоминание последнего воспроизводимого трека и позиции воспроизведения

### Как пользоваться:
1. Поместите аудиофайлы в папку `Music/`
2. Запустите приложение - музыка начнет воспроизводиться автоматически
3. Используйте кнопки управления на главном экране или откройте расширенный плеер для большего функционала
4. Добавляйте треки в избранное двойным щелчком по звездочке в плейлисте

## Кэш и обновление

Нажатие «🧹 Очистить кэш» удаляет файл `news_cache_v3.json` и очищает память. При следующем открытии статьи кэш создаётся заново. Статус-бар показывает стадии загрузки и перевода.

## Пакетирование и распространение

1. Убедитесь, что в корне проекта есть `main.py`, `logo.png`, `README.md`, `requirements.txt` и `news_cache_v3.json` (по желанию).
2. Создайте ZIP-архив, например `zip -r hytale-portal-v1.1.3-KDG.zip *` или через проводник (правой кнопкой → Отправить → Сжатая папку).
3. По желанию соберите исполняемый файл с PyInstaller (`pyinstaller --noconfirm --onefile --name "HytaleTracker" main.py`). В итоговой папке дополнительно включите `logo.png`.
4. В README укажите пользователю: распаковать, установить зависимости, запустить `python main.py`.

## Версия и авторство

- Текущая версия: **1.2.0-KDG**.
- Приложение создано KDG — https://bio.link/kdg_info.
- Главное окно открывается на весь экран, элементы адаптируются под максимум пространства. 

## Предыдущие версии

### Версия 1.3.0
- Добавлен встроенный музыкальный плеер с поддержкой плейлистов
- Добавлены кнопки управления воспроизведением
- Реализовано кэширование избранных треков

### Версия 1.2.2
- Улучшено управление музыкой: кнопка "Музыка: Вкл/Выкл" теперь корректно включает и выключает воспроизведение
- Улучшен интерфейс: кнопки управления музыкой выровнены по центру под кнопкой очистки кэша
- Добавлена музыкальная нота 🎵 к кнопке открытия плеера
- Исправлены ошибки в работе аудиоплеера

### Версия 1.2.1
- Добавлен встроенный музыкальный плеер с поддержкой форматов MP3, OGG, WAV, FLAC, AAC, M4A
- Реализовано управление громкостью и воспроизведением с главного экрана
- Добавлено сохранение избранных треков

### Версия 1.2.0
- Добавлен встроенный плеер музыки на базе `pygame`
- Реализовано определение длительности треков через `mutagen`

### Версия 1.1.4
- Исправлено отображение и открытие YouTube Shorts
- Миниатюры корректно загружаются по идентификатору видео
- Просмотр всегда открывает нормализованный `watch?v=` URL

### Версия 1.1.3
- Базовая версия со списком новостей, видео и кэшем статей
